# 项目交付检查清单

**用途**：答辩、存档或第三方核验时，按表格核对所列路径是否齐备、实验是否可追溯。

---

## 目录

- **任务书「预期功能」完成情况**（下表逐项）  
- **任务书「设计工作内容」完成情况**  
- **当前进度明细**：已完成模块表 · 仍需补强（按优先级排序）  
- **最短论文收束路线**（流程图式步骤）

---

## 任务书「预期功能」完成情况

| # | 预期功能 | 状态 | 对应材料 |
|---|----------|------|----------|
| 1 | **数据预处理与特征分析模块**：CBIS-DDSM 解析、图像标准化、ROI 提取、BI-RADS 密度分级关联 | ✅ 已实现 | `clean_cbis.py`（原始→CBIS_CLEAN）→ `build_breast_masks.py`（Otsu+形态学）→ `clean_training_labels.py`（掩膜外标签清零→CBIS_CLEAN_V2）→ `generate_captions.py`（训练描述文本）；掩膜内文字擦除（CRAFT+LaMa / SD Inpaint）已归档至 `archive/preprocessing/` |
| 2 | **可控图像生成核心模型**：文本/密度/体位条件引导，高分辨率合成 | ✅ 已实现 | `scripts/generation/run_mammo_sd15.py`（SD1.5 + LoRA + Patch-Overlap；体位/密度 CLI 条件；**默认开启** legacy 标签守护；**bg-clean** 默认关） |
| 3 | **模型训练与优化模块**：LoRA 训练、checkpoint 选择、训练日志 | ✅ 已实现 | `scripts/training/train_lora_quick.py`、`train_lora_v3.sh`；**当前推理推荐** **`outputs/lora/mammo_sd15_v4_clean/`**（干净标签）；历史 v3 见 `outputs/lora/mammo_sd15_v3_retrain_*/` |
| 4 | **合成图像质量多维评估平台**：域内 16 维规则指标（及 BRISQUE、功率谱等）+ **通用** FID/KID、PRC、`sfid_spatial768` + 可视化对比 | ✅ 已实现 | `review_generated_images.py`→`summary.json`；分项公式见 `评估标准说明.md`；双轨表述见 **`评价体系说明.md`**；FID 等与真实库对齐需 **`--real-images-dir`** |
| 5 | **可配置生成与简易交互界面**：下拉菜单、文本输入、实时提交、结果画廊 | ✅ 已实现 | `apps/app_gradio.py`（顶栏 **生成**〔**默认**：预设驱动隐藏采样参数 · **调参**：全量滑条 + 载入 `LATEST_NEXT_RUN.json`〕/ **画廊** / **评估** / **一键流水线** / **调参历史**；环境检查在「生成」页折叠区） |

---

## 任务书「设计工作内容」完成情况

| # | 工作内容 | 状态 | 说明 |
|---|----------|------|------|
| 1 | **前沿技术调研与方案论证** | ✅ | `docs/开发日志.md` 含 SDXL vs SD1.5、有无 LoRA、Patch-Overlap 方案对比；参考文献见任务书 |
| 2 | **数据集构建与预处理流水线** | ✅ | `clean_cbis.py`→ `build_breast_masks.py`→ `generate_captions.py`→ `prepare_lora_dataset.py` 形成完整管线 |
| 3 | **条件扩散模型实现与训练** | ✅ | PyTorch + diffusers；**v4** 在 **CBIS_CLEAN_V2** 上训练（`mammo_sd15_v4_clean`）；**v3** 历史对照仍可用；推理默认优先 **v4 final_lora** |
| 4 | **端到端系统集成与部署** | ✅ | FastAPI + Gradio；**`apps/start.sh`**；**并行入口**：Gradio **`subprocess`** 调用 `scripts/`（不经 REST），见 **`apps/README.md`** |
| 5 | **系统化评估与消融实验** | 🔧 进行中 | 自动评审已完备；消融表 `make_ablation_table.py` 已建；模拟专家评分 `simulated_expert_score.py` 已建；**已完成**：6 轮参数迭代收敛至 R4（Score 91.9, VL 3.0/5）+ 顾问系统修复 + 文献交叉验证；**待补**：固定 ≥30 张论文级生成批次，跑消融表，填写专家评分 |
| 6 | **设计文档编纂与成果总结** | 🔧 进行中 | `docs/用户操作手册.md` + `docs/API接口文档.md` 已完善；`README.md` 完整；**待补**：毕业论文正文、答辩 PPT、演示视频 |

---

## 当前进度明细

### ✅ 已完成或基本具备

| 模块 | 状态 | 关键文件 |
|------|------|----------|
| CBIS-DDSM 预处理与元数据结构化 | ✅ | **`datasets/CBIS_CLEAN_V2/metadata_clean.csv`**（主线）；**`datasets/CBIS_CLEAN/metadata_clean.csv`**（对照） |
| 共享核心库（参数/生成/后处理/标签守护） | ✅ | **`scripts/core/`**：`PipelineConfig`（参数单一事实来源）、`SDPipeline`（可导入生成引擎）、`postprocess_pipeline.py`、`label_guard.py`、`pipeline_orchestrator.py` |
| SD1.5 + LoRA（**v4 干净标签**，v3 历史） | ✅ | **`outputs/lora/mammo_sd15_v4_clean/final_lora`**（推荐）；**`.../mammo_sd15_v3_retrain_20260502_1723/checkpoint-3000`**（对照） |
| Patch-Overlap 生成主线 | ✅ | `run_mammo_sd15.py`；`--source-seed` 已解耦（批次源图多样性保证） |
| 16 维自动评审（含 BRISQUE/PS-β） | ✅ | `review_generated_images.py`；**流水线**与 **Gradio「评估」**（`datasets/jpeg` ≥100 张时）自动传 **`--real-images-dir`** |
| FID/KID、PRC、sfid_spatial768（`academic_metrics`） | ✅ | `piq`、`torch-fidelity`、`pytorch-fid`（见 `requirements.txt`）；`--real-images-dir`；极小样本 batch 可能对 KID/PRC-k 降级或跳过 |
| 多轮对比表 | ✅ | `compare_runs.py`、`make_ablation_table.py` |
| 消融实验框架 | ✅ | `make_ablation_table.py` + `docs/开发日志.md` |
| 模拟专家评分 | ✅ | `simulated_expert_score.py` + `docs/模拟专家评分表.md` |
| 全流程报告与参数历史（可选 NLP/VL API） | ✅ | `run_generate_eval_advise.py`（主入口）、`run_full_report.py`（包装器）、`PARAM_HISTORY.json`、`LATEST_NEXT_RUN.json`、`--rollback` / `--show-history` |
| Gradio 前端（单页生成 + Tab 拆分） | ✅ | **`apps/app_gradio.py`**、`start.sh` / `stop.sh`；默认 **全图** + 密度 **scattered**；高级滑条藏于预设绑定组件；顾问 JSON 载入以 **`run_full_report.py --from-latest-tuning`** 为主，**「调参历史」** 支持回滚；答辩前 **`python3 scripts/tools/verify_ui_wiring.py`** |
| FastAPI 后端（v2，含 SD1.5 端点） | ✅ | `apps/api_server.py` |
| 生成伪影修复 | ✅ | 量化条带（noise dithering）、边界 L 形（reflect→edge padding）、标签幻觉（post-vl-all 默认开启） |
| 用户操作手册 | ✅ | `docs/用户操作手册.md` |
| API 接口文档 | ✅ | `docs/API接口文档.md` |
| 项目结构说明 | ✅ | `README.md`、`docs/developer/项目结构说明.md` |
| 归档路线标注 | ✅ | 13 个旧脚本头部均已加 `[ARCHIVED]` 注释 |

---

## 仍需补强

| 优先级 | 缺口 | 建议操作 | 预计工作量 |
|--------|------|----------|------------|
| **高** | **论文级固定生成批次**：需固定 ≥30 张候选图（固定 seed）用于论文展示 | `run_mammo_sd15.py` + v4 LoRA + V2 CSV，`--num-images 30 --seed 2026`，后筛选 tier=1 | 30 min + 人工挑选 |
| **高** | **消融实验数据**：至少 3 组对比（基线/无LoRA/无后处理）对应 eval 结果 | `run_ablation.py` 或手跑对照 | 1–2 h |
| **高** | **模拟专家评分填写**：对筛出的 ≥10 张候选图逐项评分 | `docs/模拟专家评分表.md` | 30 min |
| **中** | **毕业论文正文** | 参考 `docs/开发日志.md` 汇整 | 论文主体 |
| **中** | **答辩 PPT + 演示视频** | 截图 + 录屏 | 2–4 h |

---

## 最短论文收束路线

```
1. 生成论文级批次（≥30张，固定 seed）
      ↓
2. review_generated_images.py（`--real-images-dir`→`academic_metrics` 与域内分项）
      ↓
3. make_ablation_table.py（汇总 ≥3 组对比：基线/无LoRA/SDXL）
      ↓
4. simulated_expert_score.py（对 top-10 张评分，得均值表）
      ↓
5. compare_runs.py（多轮参数调优趋势表）
      ↓
6. 截图：Gradio 界面 + API /docs + 评审报告 CSV 预览
      ↓
7. 整理进论文第 4（系统实现）、第 5（实验与分析）、第 6 章（总结）
```

---

*最后更新：2026-05-12*
